export type ProjectShape = {
  webapp: string;
};
export type HowSteps = {
  command: string;
  options: string;
  control: string;
};
export type FooterSites = {
  masteries: string;
  projects: string;
  threeSteps: string;
};
