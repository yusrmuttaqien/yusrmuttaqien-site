export const SPLASH_DELAY = 1000;
export enum SplashStatus {
  animateIn = 'animateIn',
  delay = 'delay',
  animateOut = 'animateOut',
  standby = 'standby',
  canceled = 'canceled',
}
export enum SplashEmitLenis {
  start = 'start',
  stop = 'stop',
}
