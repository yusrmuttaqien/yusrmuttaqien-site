import { footerLinks } from '@/app/languages/en/footer';
import { masteryLists } from '@/app/languages/en/masteries';
import { projectLists } from '@/app/languages/en/projects';

const en = {
  footer: footerLinks,
  masteries: masteryLists,
  projects: projectLists,
};

export default en;
